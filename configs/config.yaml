defaults:
    - _self_
    - general : general_default
    - model : discrete  # discrete | continuous
    - train : train_default
    - dataset : cad

hydra:
  job:
    chdir: True
  run:
    dir: ./outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}-${general.name}




